PyYAML>=6.0
pdfplumber>=0.11   # tarjetas BBVA: lectura por coordenadas (las columnas no se separan en texto plano)
# El resto de los PDFs se leen con `pdftotext` (poppler-utils o xpdf, herramienta de sistema):
#   Ubuntu/Debian:  sudo apt-get install poppler-utils
#   macOS:          brew install poppler
